import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["tailwindui.com"], // Allow images from tailwindui.com
  },
  // Other Next.js config options here, if needed
};

export default nextConfig;
